function IncreasNumber() {
    // const [count, setCount] = useState(0);
  
    return (
      <div>
        <p>You clicked times</p>
        {/* <button onClick={() => setCount(count + 1)}>Click Me</button> */}
      </div>
    );
  }
  
  export default IncreasNumber;